
project:
  name: deder-cli
  description: Deder build tool CLI
  authors:
    - Sakib Hadžiavdić
  license: Apache2
  links:
    homepage: https://github.com/sake92/deder

release:
  github:
    owner: sake92
    name: deder
    overwrite: true

distributions:
  deder:
    type: NATIVE_IMAGE
    artifacts:
      - path: target/{{distributionName}}-{{projectVersion}}-linux-x86_64
        platform: linux-x86_64
     #- path: target/{{distributionName}}-{{projectVersion}}-osx-x86_64
     #  platform: osx-x86_64
     #- path: target/{{distributionName}}-{{projectVersion}}-osx-aarch_64
     #  platform: osx-aarch_64
     #- path: target/{{distributionName}}-{{projectVersion}}-windows-x86_64.exe
     #  platform: windows-x86_64

    #brew:
    #  active: ALWAYS
    #
    #scoop:
    #  active: ALWAYS
    #  bucket:
    #    owner: yourname
    #    name: scoop-bucket

# Tag and push
#  git tag v1.0.0
#  git push origin v1.0.0
