amends "../../../../../config/DederProject.pkl"

local const frontendModules = new CreateScalaJsModules {
  root = "frontend"
  template = new {
    publish = false
    scalaVersion = "3.7.1"
    scalaJsVersion = "1.20.2"
    moduleKind = "es-module"
    linkerConfig = new {
      moduleSplitStyle = "smallest-modules"
      esVersion = "es2018"
      sourceMap = false
      jsHeader = "// My custom header\n"
    }
    deps {
      "org.scala-js::scalajs-dom::2.2.0"
    }
  }
  testTemplate = (template.asTest()) {
    deps {
      "org.scalameta::munit::1.2.1"
    }
  }
}.get

modules {
  ...frontendModules.all
}
