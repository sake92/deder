{
  "name": "scalajs-deder",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently npm:watchScalaJs vite",
    "build": "vite build",
    "preview": "vite preview",
    "watchScalaJs": "bash -c 'deder exec -m frontend -t fastLinkJs -w'"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "vite": "^4.1.0"
  }
}
