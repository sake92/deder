
localPath=../../out/server/assembly.dest/out.jar

workerThreads=32

#logLevel=debug

#JAVA_OPTS=-javaagent:../../otel.jar -Dotel.service.name=deder-server -Dotel.exporter.otlp.protocol=grpc -Dotel.exporter.otlp.endpoint=http://localhost:4317

# if you want to enable OpenTelemetry tracing, uncomment the following line and make sure to adjust the path to otel.jar if necessary
#JAVA_OPTS=-Xmx128m -javaagent:../../otel.jar -Dotel.service.name=deder-server -Dotel.exporter.otlp.protocol=grpc -Dotel.exporter.otlp.endpoint=http://localhost:4317

